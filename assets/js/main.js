function resetCounter(){currentSlideNumber=0}function next(){currentSlide.className="slide",currentSlideNumber++,currentSlide=slides[currentSlideNumber],currentSlide.className+=" active"}function previous(){currentSlide.className="slide",currentSlideNumber--,currentSlide=slides[currentSlideNumber],currentSlide.className+=" active"}function removeActive(){}var slides=document.getElementsByClassName("slide"),interval=3e3,currentSlide=slides[0],currentSlideNumber=0,pause=!1,nextBtn=document.getElementById("next"),prevBtn=document.getElementById("previous");currentSlide.className+=" active";var autoSlider=setInterval(function(){currentSlideNumber<slides.length-1?next():currentSlideNumber==slides.length-1?resetCounter():clearInterval(autoSlider)},interval);nextBtn.addEventListener();